# PDF Text Build Output

This directory is reserved for local PDF text extraction output generated by `qz-reader/scripts/extract_pdf_text.py`.

Raw extracted page text is intentionally not part of the curated shared references because the project should not redistribute the PDF as plain text. Re-run the extraction script locally when you need to rebuild source maps or audit page-level OCR/text quality.
