# qz-reader

Use `claude-code/skills/qz-reader/SKILL.md`.

Task: $ARGUMENTS

Extract and validate chart data into `qz_structured_data.md`. Reader only reads data and must not judge fortune, relationships, wealth, illness, or legal outcomes.
